
1. При создании парсер проверяет корректность оформления файла.
    1. Последовательная проверка всех символов и их позиций.
    2. Пост-проверка наличия идентичных полей в блоках. (стоит ли?)

    ! Все ошибки должны возникать на данном этапе, на втором этапе ошибки недопустимы.

2. Парсер через функцию getNextKeyValue должен парсить один из верхнеуровневых параметров.
    1. Данная функция запускается в цикле при полном парсинге файла и сохраняет данные в JsonObject.
    2. Данная функция запускается в цикле при парсинге одного конкретного элемента и заканчивает свою работу при
    его нахождении.

